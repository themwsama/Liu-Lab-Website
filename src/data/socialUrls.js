import { assetUrl } from '../lib/assetUrl.js'

/** Shared social icon paths (under `public/socialLogos/`). */
export const SOCIAL_LOGO_URLS = {
  git: assetUrl('socialLogos', 'git.svg'),
  google: assetUrl('socialLogos', 'google.svg'),
  email: assetUrl('socialLogos', 'email.svg'),
  cv: assetUrl('socialLogos', 'cv.svg'),
}
